# Backend API deps (the search engine's heavy deps live in the repo-root
# requirements.txt: crawl4ai, playwright, numpy, etc.)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.13.4
python-multipart==0.0.20
python-dotenv==1.2.2
# Used directly by llm_providers.py (the Voyage embeddings call) and by several
# fetchers. It arrives transitively via openai anyway, but depending on another
# package's dependency tree staying the same is not a dependency declaration.
httpx==0.28.1
openai==2.44.0
# OPTIONAL. Only needed if you point a model tier at a Claude model -- see
# ENGINE_*_MODEL in .env.example. llm_providers.py imports it lazily, so an
# OpenAI-only install can remove this line and nothing breaks.
anthropic==1.6.0
pdfplumber==0.11.10
python-docx==1.2.0
